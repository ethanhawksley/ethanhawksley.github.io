---
import type { GetStaticPaths } from 'astro';
import { render } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import A from '../../components/A.astro';
import { personJsonLd, websiteRef } from '../../utils/jsonLd';
import { getSortedSecondMaintainer } from '../../utils/content-helpers';

export const getStaticPaths = (async () => {
  const allChapters = await getSortedSecondMaintainer();

  return allChapters.map((chapter, index) => ({
    params: { id: chapter.id },
    props: {
      chapter,
      prevChapter: allChapters[index - 1] ?? null,
      nextChapter: allChapters[index + 1] ?? null,
    },
  }));
}) satisfies GetStaticPaths;

const { chapter, prevChapter, nextChapter } = Astro.props;
const { Content } = await render(chapter);

const chapterUrl = `https://hawksley.dev/the-second-maintainer/${chapter.id}`;
const fullTitle = `${chapter.data.index}: ${chapter.data.title}`;
const chapterDescription = `Chapter ${chapter.data.index} of The Second Maintainer: Inside the XZ Backdoor by Ethan Hawksley.`;

const ogImageUrl = new URL(
  `/og/the-second-maintainer/${chapter.id}.jpg`,
  Astro.site,
).toString();

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    personJsonLd,
    websiteRef,
    {
      '@type': 'WebPage',
      '@id': `${chapterUrl}#webpage`,
      url: chapterUrl,
      isPartOf: { '@id': 'https://hawksley.dev#website' },
      name: `${fullTitle} | The Second Maintainer`,
      description: chapterDescription,
      inLanguage: 'en',
      mainEntity: { '@id': `${chapterUrl}#chapter` },
      breadcrumb: { '@id': `${chapterUrl}#breadcrumb` },
      primaryImageOfPage: {
        '@type': 'ImageObject',
        url: ogImageUrl,
        name: `${fullTitle} - Cover Image`,
        width: 1200,
        height: 630,
      },
    },
    {
      '@type': 'Book',
      '@id': 'https://hawksley.dev/the-second-maintainer#book',
      url: 'https://hawksley.dev/the-second-maintainer',
      name: 'The Second Maintainer',
      author: { '@id': 'https://hawksley.dev#person' },
      publisher: { '@id': 'https://hawksley.dev#person' },
    },
    {
      '@type': 'Chapter',
      '@id': `${chapterUrl}#chapter`,
      url: chapterUrl,
      mainEntityOfPage: { '@id': `${chapterUrl}#webpage` },
      isPartOf: { '@id': 'https://hawksley.dev/the-second-maintainer#book' },
      name: fullTitle,
      headline: fullTitle,
      description: chapterDescription,
      position: chapter.data.index,
      inLanguage: 'en',
      author: { '@id': 'https://hawksley.dev#person' },
      publisher: { '@id': 'https://hawksley.dev#person' },
      image: {
        '@type': 'ImageObject',
        url: ogImageUrl,
        width: 1200,
        height: 630,
      },
      license: 'https://creativecommons.org/licenses/by-sa/4.0/',
    },
    {
      '@type': 'BreadcrumbList',
      '@id': `${chapterUrl}#breadcrumb`,
      itemListElement: [
        {
          '@type': 'ListItem',
          item: 'https://hawksley.dev',
          position: 1,
          name: 'Home',
        },
        {
          '@type': 'ListItem',
          item: 'https://hawksley.dev/the-second-maintainer',
          position: 2,
          name: 'The Second Maintainer',
        },
        {
          '@type': 'ListItem',
          item: chapterUrl,
          position: 3,
          name: fullTitle,
        },
      ],
    },
  ],
};
---

<Layout
  title={`${fullTitle} | Ethan Hawksley`}
  description={chapterDescription}
  jsonLd={jsonLd}
  ogType="article"
  ogImage={ogImageUrl}
>
  <div class="back-to-index">
    <A href="/the-second-maintainer" pressable>
      <span class="arrow" aria-hidden="true">←</span> Back to index
    </A>
  </div>
  <article>
    <header>
      <h1>{fullTitle}</h1>
    </header>
    <div class="prose">
      <Content />
    </div>
  </article>

  <nav class="chapter-pager" aria-label="Chapter pagination">
    <div class="pager-links">
      <div class="pager-prev">
        {
          prevChapter && (
            <a
              href={`/the-second-maintainer/${prevChapter.id}`}
              class="pager-link"
            >
              <span class="pager-link-label">
                <span class="arrow" aria-hidden="true">
                  ←
                </span>
                Previous
              </span>
              <span class="pager-link-title">
                {prevChapter.data.index}: {prevChapter.data.title}
              </span>
            </a>
          )
        }
      </div>
      <div class="pager-next">
        {
          nextChapter && (
            <a
              href={`/the-second-maintainer/${nextChapter.id}`}
              class="pager-link pager-link-next"
            >
              <span class="pager-link-label pager-link-label-next">
                Next
                <span class="arrow" aria-hidden="true">
                  →
                </span>
              </span>
              <span class="pager-link-title">
                {nextChapter.data.index}: {nextChapter.data.title}
              </span>
            </a>
          )
        }
      </div>
    </div>
    <div class="pager-view-all">
      <A href="/the-second-maintainer" pressable>Index</A>
    </div>
  </nav>
</Layout>

<style is:global>
  .prose {
    min-width: 0;
  }

  .prose > :first-child,
  .prose blockquote > :first-child {
    margin-top: 0;
  }

  .prose > :last-child,
  .prose blockquote > :last-child {
    margin-bottom: 0;
  }

  .prose h2,
  .prose h3,
  .prose h4,
  .prose h5,
  .prose h6,
  .prose p,
  .prose li,
  .prose blockquote {
    max-width: 65ch;
  }

  .prose p,
  .prose ul,
  .prose ol {
    margin-top: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .prose h2 {
    margin-top: 2.5rem;
    margin-bottom: 0.75rem;
    font-size: var(--font-size-lg);
  }

  .prose h3 {
    margin-top: 2rem;
    margin-bottom: 0.5rem;
    font-size: var(--font-size-md);
  }

  .prose h4 {
    margin-top: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .prose h5,
  .prose h6 {
    margin-top: 1.25rem;
    margin-bottom: 0.25rem;
  }

  .prose li + li {
    margin-top: 0.5rem;
  }

  .prose li > ul,
  .prose li > ol {
    margin-top: 0.5rem;
    margin-bottom: 0;
  }

  .prose img {
    border-radius: 1rem;
  }

  .prose blockquote {
    padding: 1rem 1.5rem;
    font-style: italic;
    color: var(--color-text-muted);
    background: var(--color-surface);
    border-left: 4px solid var(--color-accent);
    border-top-right-radius: 0.5rem;
    border-bottom-right-radius: 0.5rem;
  }

  @media print {
    .back-to-index,
    .chapter-pager {
      display: none !important;
    }

    .prose a[href^='http']::after {
      font-size: var(--font-size-sm);
      color: var(--color-text-muted);
      word-break: break-all;
      content: ' (' attr(href) ')';
    }

    .prose a span {
      text-decoration-thickness: 0.07em !important;
      text-underline-offset: auto !important;
    }
  }
</style>

<style>
  .arrow {
    display: inline-block;
    transition: transform 250ms ease-out;
  }

  .back-to-index {
    margin-bottom: 0.5rem;
    font-size: var(--font-size-sm);
  }

  .back-to-index :global(a) {
    color: var(--color-text-muted);
  }

  .back-to-index :global(a:hover) {
    color: var(--color-accent);
  }

  .back-to-index .arrow {
    margin-right: 0.15em;
  }

  .back-to-index :global(a:hover) .arrow {
    transform: translateX(-0.25em);
  }

  article {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  header {
    display: flex;
    flex-direction: column;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border);
  }

  .chapter-pager {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding-top: 1.5rem;
    margin-top: 1.5rem;
    border-top: 1px solid var(--color-border);
  }

  .pager-links {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .pager-link {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    color: inherit;
    text-decoration: none;
  }

  .pager-link:active {
    transform: scale(0.96);
  }

  .pager-link-label {
    display: flex;
    gap: 0.35em;
    align-items: center;
    font-family: var(--font-mono);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  .pager-link-label-next {
    justify-content: flex-end;
  }

  .pager-link-title {
    font-weight: var(--weight-medium);
    text-wrap: balance;
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-decoration-color: var(--color-accent);
    text-underline-offset: 0.25em;
  }

  .pager-link-next .pager-link-title {
    text-align: right;
  }

  .pager-link:hover .pager-link-title {
    color: var(--color-accent);
  }

  .pager-prev .pager-link:hover .arrow {
    transform: translateX(-0.25em);
  }

  .pager-next .pager-link:hover .arrow {
    transform: translateX(0.25em);
  }

  .pager-view-all {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    text-align: center;
  }

  @media (min-width: 40em) {
    .pager-links {
      display: grid;
      grid-template-columns: 1fr 1fr;
    }

    .pager-prev {
      grid-column: 1;
    }

    .pager-next {
      grid-column: 2;
    }
  }
</style>
