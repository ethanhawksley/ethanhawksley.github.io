---
import ThemeToggle from './ThemeToggle.astro';
import MonogramRound from '../assets/ethan-hawksley-monogram-round.svg';

const isHome = Astro.url.pathname === '/';
const isBlogIndex = Astro.url.pathname === '/blog/';
const isBlog = Astro.url.pathname.startsWith('/blog/');
---

<header>
  <nav aria-label="Main">
    <a href="/" aria-label="Home" class="monogram"
      ><MonogramRound width="3rem" height="3rem" aria-hidden="true" /></a
    >
    <div class="page-links">
      <a
        href="/"
        class:list={[{ active: isHome }]}
        aria-current={isHome ? 'page' : undefined}
      >
        Home</a
      >
      <a
        href="/blog/"
        class:list={[{ active: isBlog }]}
        aria-current={isBlogIndex ? 'page' : isBlog ? 'true' : undefined}
      >
        Blog</a
      >
      <ThemeToggle />
    </div>
  </nav>
</header>

<style>
  nav {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    block-size: auto;
    min-block-size: 3rem;
    padding-inline: 1.5rem;
  }

  .monogram {
    inline-size: 3rem;
    block-size: 3rem;
  }

  .page-links {
    display: flex;
    flex-direction: row;
    gap: 1.5rem;
  }

  a {
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: var(--leading-tight);
    color: inherit;
    text-decoration: none;
  }

  .page-links a {
    padding-block: 0.75rem;
    padding-inline: 0.25rem;
    margin-block: -0.75rem;
    margin-inline: -0.25rem;
  }

  .active {
    color: var(--color-accent);
    text-decoration: underline 2px;
    text-underline-offset: 0.25em;
  }

  a:hover {
    color: var(--color-accent);
  }

  a:active {
    transform: scale(0.96);
  }
</style>
